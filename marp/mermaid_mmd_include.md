---
marp: true
---

<!-- NOTE: This demo does NOT render correctly in Marp.
     Marp cannot directly include .mmd files as images.
     You must pre-render them to SVG using mmdc first:
       mmdc -i mermaid/sample.mmd -o images/sample.svg
     See mermaid_external.md for the working approach. -->

# Mermaid .mmd Include Demo

## Direct .mmd inclusion (does not work)

![Mermaid Diagram](../mermaid/sample.mmd)
